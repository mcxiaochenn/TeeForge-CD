# 私密维护资料

本目录用于存放只在用户授权的本地维护任务中读取的资料。真实文件被 Git 忽略，不得提交、复制到公开文档、日志、Issue、最终回复或自动化输出。

AI 默认不读取本目录；只有当前任务明确涉及 Keybox 私密维护且获得授权时，才按需读取具体文件。

目录中的资料不属于公开架构文档。公开内容只应引用 `docs/project/ARCHITECTURE.md` 中的数据流和安全边界。
